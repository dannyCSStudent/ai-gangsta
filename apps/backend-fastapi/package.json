{
    "name": "backend-fastapi",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "dev": "python -m uvicorn app.main:app --reload --port ${FASTAPI_PORT:-3002}",
      "start": "python -m uvicorn main:app --port ${FASTAPI_PORT:-3002}",
      "lint": "flake8",
      "test": "pytest"
    }
  }