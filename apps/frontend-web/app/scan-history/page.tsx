'use client'

import { ScreenContainer } from '@repo/ui/'

export default function ScanHistoryPage() {
  return (
    <ScreenContainer className="items-center justify-center">
      <h1 className="text-2xl font-bold">📰 Scan History (Web)</h1>
    </ScreenContainer>
  )
}
